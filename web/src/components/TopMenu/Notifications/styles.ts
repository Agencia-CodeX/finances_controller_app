import styled from "styled-components";
import { Bell } from "phosphor-react";

export const Icon = styled(Bell)`
    width: 1.5rem;
    height: 1.5rem;
    font-weight: bold;
    color: var(--white)
`;